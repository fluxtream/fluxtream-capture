/**
 * Angular controller for the configuration page
 */
define([
  'libs/jquery-2.0.2',
  'controllers/controller-module'
], function(_, controllers) {
  // Config controller
  controllers.controller('ConfigController', function($scope) {
    
  });
});
