/**
 * Angular controller for the main menu page
 */
define([
  'libs/jquery-2.0.2',
  'controllers/controller-module'
], function(_, controllers) {
  // Main menu controller
  controllers.controller('MainMenuController', function($scope) {
    
  });
});
